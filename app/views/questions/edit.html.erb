<h1> Edit Question </h1>

<div class="row">
    <div class="col-md-4">
      <p> Guidelines for questions </p>
      <ul>
        <li> Be clear. </li>
        <li> Be concise. </li>
        <li> Do a search before asking. </li>
      </ul>
    </div>

    <div class="col-md-8">
      <%= form_for @question do |q| %>
        <div class="form-group">
          <%= q.label :title %>
          <%= q.text_field :title, class: 'form-control', placeholder: "Enter question title" %>
        </div>

        <div class="form-group">
          <%= q.label :body %>
          <%= q.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter question body" %>
        </div>

        <div class="form-group">
          <%= q.submit "Save", class: 'btn btn-success' %>
        </div>

        <div class="form-group">
          <%= check_box("Question", "Resolved", {}, "yes", "no") %>
          Question Resolved?  
      <% end %>
    </div>
  </div>